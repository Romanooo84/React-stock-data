import { useState, useEffect } from "react"

export const Header=({multiplyData})=>{
    useEffect(() => {
        
        if (multiplyData) {
        }
      }, []);

    return(
        <header>
            to jest nagłówek
        </header>
    )
}